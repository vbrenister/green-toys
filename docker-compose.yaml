version: '3.9'

services:
  postgres:
    image: postgres:14-alpine
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: green_toys_user
      POSTGRES_PASSWORD: password
      POSTGRES_DB: green_toys_db
